server:
  port: 9410

spring:
  application:
    name: zipkinstreamserver
  rabbitmq:
    host: ${RABBIT_HOST:192.168.1.211}
    port: 5672
    username: test
    password: test
    virtual-host: test
  datasource:
    initialize: false
  profiles:
    active: mysql



---
spring:
  profiles: mysql
  datasource:
    #schema: classpath:/mysql.sql
    url: jdbc:mysql://${MYSQL_HOST:192.168.1.204:3306}/cloud_auth
    username: cloud
    password: cloud
# Switch this on to create the schema on startup:
    #initialize: true
    continueOnError: true
  sleuth:
    enabled: false
zipkin:
  storage:
    type: mysql # default is mem (in-memory)



