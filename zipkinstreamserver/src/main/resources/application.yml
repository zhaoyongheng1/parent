server:
  port: 9410

spring:
  application:
    name: zipkinstreamserver
  rabbitmq:
    host: ${RABBIT_HOST:192.168.2.231}
    port: 5672
    username: wcbtest
    password: wcbtest
    virtual-host: wcbtest
  datasource:
    initialize: false
  profiles:
    active: mysql


---
spring:
  profiles: mysql
  datasource:
    #schema: classpath:/mysql.sql
    url: jdbc:mysql://${MYSQL_HOST:192.168.2.172}/waikuai
    username: root
    password: root
# Switch this on to create the schema on startup:
    #initialize: true
    continueOnError: true
  sleuth:
    enabled: false
zipkin:
  storage:
    type: mysql # default is mem (in-memory)


